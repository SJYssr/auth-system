# e2e 测试专用环境：务必指向独立的测试数据库，不要指向生产库！
# 复制为 e2e-test/.env 后按实际修改；未创建该文件时回退读 server/.env
E2E_BASE=http://localhost:3001
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=auth_admin
DB_PASSWORD=your_password
DB_NAME=auth-system
